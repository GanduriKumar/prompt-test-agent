# Playwright is a library for browser automation, useful for testing web applications.
playwright~=1.56.0

# Ollama is a package for working with machine learning models, providing an interface for model management.
ollama~=0.6.1

# Python-dotenv is used to load environment variables from a .env file, helping manage configuration settings.
python-dotenv~=1.2.1

# Certifi provides a curated collection of root certificates for validating the trustworthiness of SSL certificates.
certifi~=2025.11.12

# Python-certifi-win32 is a Windows-specific package that provides the same functionality as certifi for Windows users.
python-certifi-win32~=1.6.1

# Requests is a simple and elegant HTTP library for Python, making it easy to send HTTP requests.
requests~=2.32.5

# OpenAI Python library for GPT-4 and other OpenAI models
openai~=2.9.0

# Anthropic Python library for Claude models
anthropic~=0.39.0

# Google Generative AI library for Gemini models
google-generativeai~=0.8.3